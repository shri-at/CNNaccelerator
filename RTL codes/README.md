PFPM module is the core computational module of the convolutional layer  
It takes in kernels and input matrices to produce partial feature maps, Once corresponding partial feature maps of all iput depth is calculated theya re added together to obtain the complete feature map along with bias matrix  

